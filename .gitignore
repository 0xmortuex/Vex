node_modules/
dist/
.claude/
.ruflo/
.octogent/
# Icon build intermediates (regenerated from assets/icon.svg by `npm run build-icons`)
assets/icon-source.png
assets/icons/
.tmp-asar/

.octogent

# Theme assets that may have IP concerns — never commit, even on accidental `git add .`
assets/cia-seal.png

# Local sidebar config — may carry a personalized URL with a secret query
# param. Lives in <userData>/sidebar-config.json; copy sidebar-config.example.json
# to set it up. The bare name matches the file at any depth (incl. repo root).
sidebar-config.json
